export const API_URL=process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:8000'; export type Turn={speaker:string;text:string}; export async function api<T>(path:string,init?:RequestInit):Promise<T>{const r=await fetch(`${API_URL}${path}`,{...init,headers:{'Content-Type':'application/json',...(init?.headers??{})},cache:'no-store'}); if(!r.ok) throw new Error(await r.text()); return r.json() as Promise<T>} export function wsUrl(jobId:string){return `${API_URL.replace('http','ws')}/ws/progress/${jobId}`}
