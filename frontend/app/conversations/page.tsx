"use client"; import { useEffect,useState } from 'react'; import { Shell } from '@/components/Shell'; import { api } from '@/lib/api'; export default function Conversations(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api<any[]>('/api/conversations').then(setRows).catch(()=>setRows([]))},[]);return <Shell><h1 className="text-[32px] font-semibold">Conversations</h1><p className="mt-2 text-lg text-muted">Review saved dialogue datasets and evaluation inputs.</p><div className="mt-8 grid gap-4">{rows.map(r=><article className="panel p-5" key={r.id}><h2 className="font-semibold">{r.title}</h2><div className="mt-2 font-mono text-xs text-muted">{r.turns.length} turns / {r.tags.join(', ')}</div></article>)}</div></Shell>}
