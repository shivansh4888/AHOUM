"use client"; import { Bar,BarChart,ResponsiveContainer,XAxis,YAxis,Tooltip } from 'recharts'; export function DomainOverview({scores}:{scores:any[]}){const m=new Map<string,{score:number;count:number}>();scores.forEach(s=>{const row=m.get(s.domain)??{score:0,count:0};row.score+=Number(s.quality_score ?? s.display_score ?? s.score ?? 0);row.count++;m.set(s.domain,row)});const data=Array.from(m.entries()).map(([domain,row])=>({domain,score:Number((row.score/row.count).toFixed(2))}));return <div className="h-64"><ResponsiveContainer><BarChart data={data}><XAxis dataKey="domain" stroke="#737373" tick={{fontSize:12}}/><YAxis domain={[-2,2]} stroke="#737373" tick={{fontSize:12}}/><Tooltip contentStyle={{background:'#FFFFFF',border:'1px solid #E5E5E5',borderRadius:8,color:'#171717'}}/><Bar dataKey="score" fill="#2563EB" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div>}
