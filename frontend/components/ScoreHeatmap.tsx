"use client";
export function ScoreHeatmap({scores}:{scores:any[]}){return <div className="grid grid-cols-2 gap-3 md:grid-cols-4 xl:grid-cols-6">{scores.map((s,i)=>{const value=Math.round(Number(s.display_score ?? s.score ?? 0));const positive=s.score_kind==='risk'?value<=0:value>=0;return <div key={i} className={`rounded-lg border bg-surface p-3 dark:bg-neutral-900 ${positive?'border-success/20':'border-rose/20'}`}><div className="truncate text-xs text-muted">{s.facet_name}</div><div className={`mt-2 font-mono text-2xl font-semibold ${positive?'text-success':'text-rose'}`}>{value}</div></div>})}</div>}
